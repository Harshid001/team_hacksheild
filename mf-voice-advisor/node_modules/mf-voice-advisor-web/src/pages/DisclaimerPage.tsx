import { useNavigate } from 'react-router-dom';
import { DisclaimerBanner } from '../components/DisclaimerBanner';
import { ShieldCheck, ArrowRight } from 'lucide-react';

export function DisclaimerPage() {
  const navigate = useNavigate();

  return (
    <div className="min-h-screen flex items-center justify-center p-6 relative z-10">
      <div className="max-w-2xl w-full animate-in fade-in slide-in-from-bottom-8 duration-700">
        {/* Header */}
        <div className="text-center mb-12">
          <div className="inline-flex items-center justify-center w-20 h-20 rounded-full bg-indigo-500/20 border border-indigo-400 mb-6 shadow-[0_0_30px_rgba(99,102,241,0.3)]">
            <ShieldCheck size={40} className="text-indigo-400" />
          </div>
          <h1 className="text-5xl font-bold tracking-tight mb-4 text-gradient">
            MF Voice Advisor
          </h1>
          <p className="text-xl text-slate-400">
            AI-Powered Mutual Fund Analysis
          </p>
        </div>

        {/* Disclaimer */}
        <div className="mb-12">
          <DisclaimerBanner />
        </div>

        {/* Action */}
        <div className="text-center">
          <button
            onClick={() => navigate('/conversation')}
            className="btn-primary group flex items-center gap-3 mx-auto text-lg px-8 py-4"
          >
            I Understand — Begin
            <ArrowRight size={20} className="group-hover:translate-x-1 transition-transform" />
          </button>
        </div>
      </div>
    </div>
  );
}
